refresh_rate: 30 seconds
appenders:
  stdout:
    kind: console
  appli:
    kind: file
    path: "log/logger.log"
    encoder:
      pattern: "{d} - {m}{n}"
root:
  level: debug
  appenders:
    - stdout
loggers:
  app:
    level: debug
    appenders:
      - appli
    additive: false